package org.example.relik.modelo;

import org.example.relik.dominio.RestoMaterial;
import org.springframework.data.jpa.repository.JpaRepository;

public interface RestoMaterialRepository extends JpaRepository<RestoMaterial, Integer> {
}

